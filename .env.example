# AI EASE Proxy Configuration
# 初次运行时，访问 http://localhost:3001 将自动进入初始化配置界面
# 配置会自动生成此文件，无需手动创建

# Server Configuration
PORT=3001
HOST=0.0.0.0

# Multi-user mode (true/false)
# 开启后需要注册/登录才能使用
MULTI_USER_MODE=true

# JWT Secret (初始化时自动生成，也可手动更改)
JWT_SECRET=your-super-secret-jwt-key-change-this

# Database Type: sqlite / mysql
# 本地开发默认 sqlite；Docker/生产环境建议 mysql
DB_TYPE=sqlite

# SQLite Storage Path (仅 DB_TYPE=sqlite 时生效)
DB_STORAGE=./database.sqlite

# MySQL Database Configuration (仅 DB_TYPE=mysql 时生效)
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=aiease_proxy
# DB_USER=root
# DB_PASSWORD=your-secure-password

# Max concurrent upstream requests
MAX_CONCURRENT_UPSTREAM=10
