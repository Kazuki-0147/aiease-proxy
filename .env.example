# AI EASE Proxy Configuration Template
# 复制此文件为 .env 并填入真实值

# ==================== 服务器配置 ====================
PORT=3001
HOST=0.0.0.0
NODE_ENV=production

# ==================== 多用户模式 ====================
# 是否启用多用户系统 (true/false)
MULTI_USER_MODE=true

# ==================== JWT 密钥 ====================
# ⚠️ 安全警告：请使用强随机密钥，至少 64 字符
# 生成命令: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_please_change_this

# ==================== 数据库配置 ====================
# 数据库类型: sqlite / mysql
DB_TYPE=sqlite

# SQLite 配置 (当 DB_TYPE=sqlite 时使用)
DB_STORAGE=./database.sqlite

# MySQL 配置 (当 DB_TYPE=mysql 时使用)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=aiease_proxy
DB_USER=root
DB_PASSWORD=your_mysql_password_here

# ==================== 生产环境数据库同步 ====================
# ⚠️ 警告：生产环境使用 sync({ alter: true }) 存在风险
# 强烈建议使用数据库迁移工具（sequelize-cli 或 umzug）
# 如需强制启用 sync，设置为 true（不推荐）
ALLOW_SYNC_IN_PRODUCTION=false

# ==================== 并发控制 ====================
# 上游 API 最大并发请求数
MAX_CONCURRENT_UPSTREAM=10
